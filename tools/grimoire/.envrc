use flake

# Optional: Watch additional files for auto-reload
watch_file pyproject.toml
watch_file uv.lock

# Optional: Load local environment variables if present
if [ -f .env.local ]; then
  dotenv .env.local
fi